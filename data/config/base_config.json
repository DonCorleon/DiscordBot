{
  "_comment": "Global configuration overrides (nested format)",
  "_format": "CogName -> setting_key -> value",
  "_note": "Only include settings that differ from defaults defined in code",
  "System": {
    "log_level": "INFO",
    "web_reload": true,
    "enable_web_dashboard": true,
    "web_port": 8500
  },
  "Voice": {
    "auto_join_timeout": 30
  }
}